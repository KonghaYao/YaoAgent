{
	"name": "cloudflare",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"@langgraph-js/crawler": "^1.4.6",
		"hono": "^4.7.11"
	},
	"devDependencies": {
		"wrangler": "^4.18.0"
	}
}